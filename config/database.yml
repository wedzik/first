defaults: &defaults
  adapter: postgresql
  encoding: unicode
  pool: 5
  username: postgres
  password: admin

development:
  <<: *defaults
  database: first_development

test:
  <<: *defaults
  database: first_test

production:
  <<: *defaults
  database: first_production